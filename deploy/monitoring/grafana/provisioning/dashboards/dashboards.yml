apiVersion: 1

providers:
  - name: browser-gateway
    orgId: 1
    folder: Browser Gateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
