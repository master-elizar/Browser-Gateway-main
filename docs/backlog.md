# Backlog

Stage 16 / 0.17.x is shipped. Ideas below are **not** scheduled:

- Let’s Encrypt / ACME automation (replace self-signed)
- MFA / OIDC
- Multi-node orchestration
- Network waterfall / timing view
- More TI feeds (Google Safe Browsing, MISP, commercial)
- History export (zip of frames + network dump)
- Persistent browser profiles (explicitly out of MVP; revisit only with strong isolation story)
- PCAP download / video session recording
