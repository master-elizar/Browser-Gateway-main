# Stage 3 — Frontend shell

## Delivered

- React 19 + TypeScript + Vite 6 + Tailwind CSS 4
- i18n EN / RU
- Routes: Login, Sessions, Session viewer (toolbar + net panel placeholder), Admin stubs
- API client pointed at real `/api/*` endpoints
- Stage 3 **preview mode** to explore UI until Stage 4 auth lands
- Multi-stage Docker build → nginx

## Host capacity (after resize)

| Resource | Value |
|----------|-------|
| RAM | 15 GiB |
| Root disk | ~97 GiB (~75 GiB free) |

## Verify

Open `https://<host>/` → Login → **Explore UI (Stage 3 preview)**.
